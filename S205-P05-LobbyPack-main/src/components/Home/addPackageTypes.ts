// Tipos de datos para el formulario de agregar un nuevo paquete.

export type AddPackageFormValues = {
  apartment: string;
  residentName: string;
  phone: string;
  company: string;
  concierge: string;
};
