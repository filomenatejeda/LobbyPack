[build]
builder = "RAILPACK"
buildCommand = "cd backend && bun install --frozen-lockfile"

[deploy]
startCommand = "cd backend && bun run start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
